import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  inner: {
    width: 200,
    height: 200,
  },
  outer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
