import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  inner: {
    color: '#333',
    padding: 10,
  },
  outer: {
    backgroundColor: '#fff',
    height: 44,
    margin: 12,
    borderWidth: 1,
  },
});
